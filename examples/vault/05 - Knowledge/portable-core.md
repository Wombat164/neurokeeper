---
note_type: knowledge
status: active
maturity: current
horizon: timeless
tags: [demo, architecture]
---

# Portable Core

The load-bearing logic lives in deterministic engines + prompt templates + versioned contracts; thin
adapters bind them to a runtime. See [[sample-project]] for a worked example and [[migration-plan]] for
the rollout shape.
