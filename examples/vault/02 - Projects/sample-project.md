---
note_type: project
status: active
maturity: current
horizon: near
tags: [demo, portable-core]
---

# Sample Project

A worked example note. It links to [[migration-plan]] and the [[portable-core]] concept, and embeds a
diagram: ![[diagram.png]].

It also forward-links to [[future-idea]] -- a note that does not exist yet. That is an intentional
Obsidian forward-reference: `ref-audit` reports it as an unresolved link but does not treat it as an
error (and `doctor --check` stays green).
